CPSC 456 Project 1: Relicator Worm
	
	Members: David Poss, Gerard Cortes, Billy Dang

	Usage: $python replicator.py <username file> <password file>

	Notes: The worm will leave a marker "gotcha.txt" file that says which IP infected that machine. Also, 
               run the script with all files (worm, username and password files) in the same directory.

